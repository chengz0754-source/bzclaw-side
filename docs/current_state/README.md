# Current State Contract Entry

This folder documents the frozen current-state role for `bzclaw-side`.

The active repo-visible current-state hosts remain:

- `README.md`
- `reports/latest_sellersprite_stage_status.json`

Automated candidate imports may stage reviewable current-state candidates under
`docs/current_state/candidates/`, but they must not overwrite the active hosts
directly.
