# Truth Pack Candidate Staging Root

This folder receives deterministic truth-pack candidate imports only.

Each candidate import should materialize:

- `<candidate_id>.candidate.json`
- `<candidate_id>.payload.json`

The active truth-pack host remains
`reports/sellersprite_truth_pack_current.json`.
