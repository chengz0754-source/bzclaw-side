# Board Candidate Staging Root

This folder receives deterministic board candidate imports only.

Each candidate import should materialize:

- `<candidate_id>.candidate.json`
- `<candidate_id>.payload.csv|json|md`

The active board host remains
`reports/selection/MASTER_PROGRESS_BOARD__20260412.csv`.
